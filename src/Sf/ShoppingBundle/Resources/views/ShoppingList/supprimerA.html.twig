{% extends 'SfShoppingBundle::layout.html.twig' %}

{% block body %}
    <h1>Supprimer un article</h1>
    <p>
    Etes-vous certain de vouloir supprimer l'article' "{{ entity.name }}" ?
  </p>

            <a href="{{ path('shoppinglist_show',  {'id': shoppingList}) }}">
                Back to the list
            </a>

    <form action="{{ path('article_delete', {'id': entity.id}) }}" method="post">
    <input type="submit" value="Supprimer" class="btn btn-danger" />
    {{ form_rest(form) }}
  </form>

{% endblock %}