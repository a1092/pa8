{% trans_default_domain 'FOSUserBundle' %}

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Kylin // Inscription</title>

  <!-- Bootstrap core CSS -->
  <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">

  <!-- Custom styles for this template -->
  <link href="{{ asset('css/style.css') }} " rel="stylesheet">

  <!-- Just for debugging purposes. Don't actually copy this line! -->
  <!--[if lt IE 9]><script src="../../assets/js/ie8-responsive-file-warning.js"></script><![endif]-->

  <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
  <![endif]-->

</head>
<body class="connexion">

  <div class="container">
    <div class="row">
      <div class="col-md-4"></div>
      <div class="col-md-4">
        <img src="{{ asset('img/logo.png') }}" class="img-responsive" />
      </div>
    </div><!-- /.row -->
    <div class="row connexion-form">
      <div class="col-md-6 col-md-offset-3">
        <div class="panel">
          <div class="panel-body">
            <h2>Créer un nouveau foyer</h2>
            <p class="bg-danger">{{ form_errors(form) }}</p>
            <h3>Mon compte</h3>
			<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} class="form-horizontal" method="POST" class="fos_user_registration_register"role="form">
              <div class="form-group">
                <label for="username" class="col-sm-3 control-label">Pseudo</label>
                <div class="col-sm-9">
				  {{ form_widget(form.username) }}
                  <span class="help-block">{{ form_errors(form.username) }}</span>
                </div>
              </div><!-- /.form-group -->
              <div class="form-group">
                <label for="firstname" class="col-sm-3 control-label">Prénom</label>
                <div class="col-sm-9">
                  {{ form_widget(form.firstname) }}
                  <span class="help-block">{{ form_errors(form.firstname) }}</span>
                </div>
              </div><!-- /.form-group -->
              <div class="form-group">
                <label for="lastname" class="col-sm-3 control-label">Nom</label>
                <div class="col-sm-9">
                  {{ form_widget(form.lastname) }}
                  <span class="help-block">{{ form_errors(form.lastname) }}</span>
                </div>
              </div><!-- /.form-group -->
              <div class="form-group">
                <label for="email" class="col-sm-3 control-label">Email</label>
                <div class="col-sm-9">
                  {{ form_widget(form.email) }}
                  <span class="help-block">{{ form_errors(form.email) }}</span>
                </div>
              </div><!-- /.form-group -->
              <div class="form-group">
                <label for="first" class="col-sm-3 control-label">Mot de passe</label>
                <div class="col-sm-9">
                 {{ form_widget(form.plainPassword.first) }}
                  <span class="help-block">{{ form_errors(form.plainPassword.first) }}</span>
                </div>
              </div><!-- /.form-group -->
              <div class="form-group">
                <label for="second" class="col-sm-3 control-label">Confirmation</label>
                <div class="col-sm-9">
                  {{ form_widget(form.plainPassword.second) }}
                  <span class="help-block">{{ form_errors(form.plainPassword.second) }}</span>
                </div>
              </div><!-- /.form-group -->
              <div class="form-group">
                <label for="sexe" class="col-sm-3 control-label">Sexe</label>
                <div class="col-sm-9">
                   {{ form_widget(form.gender) }}
                  <span class="help-block">{{ form_errors(form.gender) }}</span>
                </div>
              </div><!-- /.form-group -->
              <div class="form-group">
                <label for="avatar" class="col-sm-3 control-label">Avatar</label>
                <div class="col-sm-9">
                  {{ form_widget(form.image) }}
                  <span class="help-block">{{ form_errors(form.image) }}</span>
                </div>
              </div><!-- /.form-group -->
              <div class="form-group">
                <label for="name" class="col-sm-3 control-label">Couleur</label>
                <div class="col-sm-9">
                  {{ form_widget(form.color) }}
                  <span class="help-block">{{ form_errors(form.color) }}</span>
                </div>
              </div><!-- /.form-group -->
              <h3>Mon foyer</h3>
              <div class="form-group">
                <label for="name-foyer" class="col-sm-3 control-label">Nom</label>
                <div class="col-sm-9">
				  <input type="text" class="form-control" placeholder="Nom" id="fos_user_registration_form_foyers_Nom_name" name="fos_user_registration_form[foyers][Nom][name]" required="required">
                  <span class="help-block"></span>
                </div>
              </div><!-- /.form-group -->

              <script src="http://code.jquery.com/jquery-1.8.2.min.js"></script>

              <script type="text/javascript">
                /* $(document).ready(function() {
                  $container = $('div#fos_user_registration_form_foyers');
					
                  var index = $container.find(':input').length;
                  if (index == 0) {
                    ajouterFoyer($container);
                  }

                  function ajouterFoyer($container) {
                    $prototype = $($container.attr('data-prototype').replace(/__name__label__/g, ' ')
                      .replace(/__name__/g, 'Nom'));
					alert($prototype.html());
                    $container.append($prototype);
                    index++;
                  }
                }); */
              </script>

              <p class="text-center">
                <button type="submit" class="btn btn-success">
                  <span class="glyphicon glyphicon-ok"></span> Enregistrer
                </button>
              </p> 
				{{ form_rest(form) }}			  
            </form>      
          </div>
        </div>
      </div>
    </div><!-- /.row -->
  </div><!-- /.container -->

  <!-- Bootstrap core JavaScript
  ================================================== -->
  <!-- Placed at the end of the document so the pages load faster -->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
  <script src="{{ asset('js/bootstrap.min.js') }}"></script>
  <script src="{{ asset('js/bootstrap.file-input.js') }}"></script>
  <script>$(document).ready(function(){$('input[type=file]').bootstrapFileInput();});</script>
</body>
</html>