{% extends 'SfLoanBundle::layout.html.twig' %}

{% block body -%}
    <h1>Loan creation</h1>

    <form action="{{ path('loan_new') }}" method="post">
    	{{ form_rest(form) }}
    	<input type="submit" value="Créer" />
  	</form>

    <li>
        <a href="{{ path('loan') }}">
            Back to the list
        </a>
    </li>

{% endblock %}
